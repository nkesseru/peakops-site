rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Ingest bucket rules (tighten by path if you want)
    match /{allPaths=**} {
      allow read: if request.auth != null;   // or false for fully private
      allow write: if false;                 // no public writes
    }
  }
}
