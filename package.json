{
  "name": "my-app",
  "private": true,
  "type": "module",
  "scripts": {
    "stop": "pkill -f node || true",
    "dev": "npm run stop && node --env-file=.env functions/server.mjs",
    "seed": "node scripts/seedFirestore.mjs",
    "deploy:db": "firebase deploy --only firestore:rules,firestore:indexes"
  }
}
